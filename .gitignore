*.log

# Raw HTML dump from the Sametham crawl. Regenerable, large, and not source.
# The parser is iterated against this cache, but it does not belong in git.
data/cache/

__pycache__/
*.py[cod]

# Malayalam fonts copied from the system font directory on first run by
# rti_app/generate_rti.py. Regenerable, and not ours to vendor.
rti_app/fonts/
